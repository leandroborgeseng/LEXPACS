# Produção: servidor de imagens sem API HTTP exposta no host.
# Uso: docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d

services:
  server:
    ports:
      - "4242:4242"

  portal:
    environment:
      COOKIE_SECURE: ${COOKIE_SECURE:-true}
